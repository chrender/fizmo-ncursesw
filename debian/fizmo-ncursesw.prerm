#! /bin/sh
# prerm script for fizmo-ncursesw

set -e

case "$1" in
    remove|upgrade|deconfigure)
        update-alternatives --remove zcode-interpreter /usr/games/fizmo-ncursesw-launcher
        ;;
    failed-upgrade)
        update-alternatives --remove zcode-interpreter /usr/games/fizmo-ncursesw-launcher
        ;;
    *)
        echo "prerm called with unknown argument \`$1'" >&2
        exit 0
    ;;
esac

#DEBHELPER#

exit 0
